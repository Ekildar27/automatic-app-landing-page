# Ekildar Apps 站点操作指南

面向日常维护：加 App、改文案、管隐私/条款、写更新说明、部署上线。

生产地址：https://ekildar.kdns.fr  
仓库：`Ekildar27/automatic-app-landing-page`（生产分支 **`master`**）

---

## 目录

1. [快速对照表](#1-快速对照表)
2. [本地预览与发布](#2-本地预览与发布)
3. [添加 / 修改 / 删除 App](#3-添加--修改--删除-app)
4. [隐私政策与使用条款](#4-隐私政策与使用条款)
5. [What's New（更新说明）](#5-whats-new更新说明)
6. [中英文切换文案](#6-中英文切换文案)
7. [截图与预览视频](#7-截图与预览视频)
8. [全站配置](#8-全站配置)
9. [自定义域名](#9-自定义域名)
10. [向原开源项目贡献](#10-向原开源项目贡献)
11. [常见问题](#11-常见问题)

---

## 1. 快速对照表

| 你想做的事 | 改哪里 | 备注 |
|------------|--------|------|
| 新增一个 App | `_apps/<slug>.md` | slug = 文件名，决定 URL |
| 改 App 简介 / 功能点 | 对应 `_apps/*.md` | 见第 3 节 |
| 改首页标题 / 副标题 | `_config.yml` | 含中英文 |
| 隐私政策 / 使用条款 | `_legal/<slug>/privacy.md`、`terms.md` | 正文模板在 `_includes/` |
| 改条款占位正文（所有 App 共用结构） | `_includes/legal-privacy-placeholder.html`、`legal-terms-placeholder.html` | 上架前换成正式文案 |
| 本地写更新说明 | `_apps/<slug>.md` 里的 `whats_new:` | 有本地则不拉 App Store |
| 自动用 App Store 更新说明 | 不写 `whats_new`，正文也留空 | 详情页自动拉取 |
| 本地截图 | `assets/screenshots/<slug>/` | 优先于 App Store 截图 |
| 本地预览视频 | `assets/videos/<slug>/` | 有视频则不走截图轮播 |
| 主题色 / 页脚社交 | `_config.yml` | |
| 发布上线 | `git push` 到 `master` | GitHub Actions 自动部署 |
| 更新上游 PR | `bash scripts/contribute-upstream.sh` | 会去掉个人 App |

### 当前 App 与链接

| App | 配置文件 | 详情页 | 隐私政策 | 使用条款 |
|-----|----------|--------|----------|----------|
| Nexus Note | `_apps/nexus-note.md` | [/apps/nexus-note/](https://ekildar.kdns.fr/apps/nexus-note/) | [/apps/nexus-note/privacy/](https://ekildar.kdns.fr/apps/nexus-note/privacy/) | [/apps/nexus-note/terms/](https://ekildar.kdns.fr/apps/nexus-note/terms/) |
| Moments with Mom | `_apps/moments-with-mom.md` | [/apps/moments-with-mom/](https://ekildar.kdns.fr/apps/moments-with-mom/) | [/apps/moments-with-mom/privacy/](https://ekildar.kdns.fr/apps/moments-with-mom/privacy/) | [/apps/moments-with-mom/terms/](https://ekildar.kdns.fr/apps/moments-with-mom/terms/) |
| Animal Zoom | `_apps/animal-zoom.md` | [/apps/animal-zoom/](https://ekildar.kdns.fr/apps/animal-zoom/) | [/apps/animal-zoom/privacy/](https://ekildar.kdns.fr/apps/animal-zoom/privacy/) | [/apps/animal-zoom/terms/](https://ekildar.kdns.fr/apps/animal-zoom/terms/) |
| Animal Zoom Pro | `_apps/animal-zoom-pro.md` | [/apps/animal-zoom-pro/](https://ekildar.kdns.fr/apps/animal-zoom-pro/) | [/apps/animal-zoom-pro/privacy/](https://ekildar.kdns.fr/apps/animal-zoom-pro/privacy/) | [/apps/animal-zoom-pro/terms/](https://ekildar.kdns.fr/apps/animal-zoom-pro/terms/) |

更新日志汇总页：[/changelog/](https://ekildar.kdns.fr/changelog/)

---

## 2. 本地预览与发布

### 本地预览

需要 Ruby 3.x（推荐 Homebrew 的 Ruby）。

```bash
export PATH="/opt/homebrew/opt/ruby/bin:$PATH"
cd app-landing-page

# 首次：安装依赖
bundle install

# 本地预览（http://127.0.0.1:4000）
bundle exec jekyll serve --config _config.yml,_config_dev.yml --host 127.0.0.1 --port 4000
```

`_config_dev.yml` 会覆盖生产域名，方便本地调试。

### 发布到线上

```bash
git add -A
git commit -m "说明这次改了什么"
git push origin master
```

推送后 GitHub Actions 自动构建并部署到 https://ekildar.kdns.fr 。一般 1～2 分钟生效。手机若看不到变化，强制刷新或清缓存（CSS 带版本号，通常刷新即可）。

---

## 3. 添加 / 修改 / 删除 App

### 3.1 添加新 App

1. 在 `_apps/` 新建文件，文件名即 URL 中的 slug。  
   例：`_apps/my-cool-app.md` → 页面 `/apps/my-cool-app/`

2. 按下面模板填写（复制后改）：

```yaml
---
title: My Cool App
layout: app
app_name: My Cool App
ios_app_id: 1234567890
ios_app_country: us
app_description: English one-line description for the homepage and detail page.
app_description_zh: 中文一句话简介（首页切到「中文」时显示）。
device_color: black
features:
  - title: Feature one
    description: What this feature does.
    fontawesome_icon_name: star
  - title: Feature two
    description: Another highlight.
    fontawesome_icon_name: heart
---
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `layout: app` | 是 | 固定用详情页布局 |
| `ios_app_id` | 是 | App Store 数字 ID（在 App Store Connect 或商店链接里可见） |
| `app_name` / `title` | 建议 | 名称；也可被 iTunes API 覆盖为空时补全 |
| `app_description` | 建议 | 英文简介 |
| `app_description_zh` | 建议 | 中文简介；不写则中文模式仍显示英文 |
| `ios_app_country` | 否 | 默认 `us`；中国区用 `cn` |
| `device_color` | 否 | 手机框颜色：`black` / `blue` / `yellow` / `coral` / `white` |
| `features` | 否 | 详情页功能列表；图标名用 [Font Awesome 5](https://fontawesome.com/v5/search?m=free) 免费图标名 |
| `whats_new` | 否 | 见第 5 节 |

3. **同时创建法律页**（否则页脚链接会 404）：

```bash
mkdir -p _legal/my-cool-app
```

新建 `_legal/my-cool-app/privacy.md`：

```yaml
---
layout: legal
title: Privacy Policy
title_zh: 隐私政策
app_slug: my-cool-app
app_name: My Cool App
doc_type: privacy
---
```

新建 `_legal/my-cool-app/terms.md`：

```yaml
---
layout: legal
title: Terms of Use
title_zh: 使用条款
app_slug: my-cool-app
app_name: My Cool App
doc_type: terms
---
```

注意：`app_slug` 必须与 `_apps/` 文件名（不含 `.md`）一致。

4. 提交并 push `master`，首页会自动出现新卡片。

### 3.2 修改已有 App

直接编辑对应 `_apps/<slug>.md`，改简介、功能、颜色、国家等，保存后 push。

图标、价格、商店链接一般由 `ios_app_id` 通过 iTunes API 自动填充，通常不用手写。

### 3.3 删除 App

1. 删除 `_apps/<slug>.md`
2. 删除 `_legal/<slug>/` 整个目录（含 privacy / terms）
3. （可选）删除 `assets/screenshots/<slug>/`、`assets/videos/<slug>/`
4. push `master`

---

## 4. 隐私政策与使用条款

### 结构说明

| 角色 | 路径 |
|------|------|
| 每个 App 的页面入口 | `_legal/<slug>/privacy.md`、`terms.md`（只有 front matter） |
| 真正显示的正文模板 | `_includes/legal-privacy-placeholder.html`、`legal-terms-placeholder.html` |
| 布局（含 EN/中文切换） | `_layouts/legal.html` |

详情页页脚会自动出现 **Privacy Policy** / **Terms of Use**（中文模式下为「隐私政策」「使用条款」）。

App Store 审核可直接填：

- `https://ekildar.kdns.fr/apps/<slug>/privacy/`
- `https://ekildar.kdns.fr/apps/<slug>/terms/`

### 用法：只改某个 App 的名称显示

改该 App 的 `_legal/<slug>/*.md` 里的 `app_name` 即可（模板里会代入名称）。

### 用法：换成正式法律文案（推荐）

**方式一：某 App 独立正文（Nexus Note 已采用）**

1. 把中英双语内容写成 include，例如 `_includes/legal-nexus-note-privacy.html`（需含 `data-lang="en"` / `data-lang="zh"` 两个 `.legalPanel`）
2. 在 `_legal/<slug>/privacy.md`（或 `terms.md`）front matter 下方写：

```liquid
{% include legal-nexus-note-privacy.html %}
```

有正文时，布局会优先显示该内容，不再用占位模板。

**方式二：仍用全站占位模板**

编辑：

1. `_includes/legal-privacy-placeholder.html`
2. `_includes/legal-terms-placeholder.html`

把英文区（`data-lang="en"`）和中文区（`data-lang="zh"`）换成正式内容。  
文中的 `{{ include.app_name }}` 会自动变成各 App 名称。

### 用法：语言切换

法律页右上角 **EN / 中文**；与首页共用同一浏览器记忆，默认英文。

---

## 5. What's New（更新说明）

有三种来源，**优先级从高到低**：

1. `_apps/<slug>.md` 里配置 `whats_new:`（YAML）
2. 同一文件 front matter **下方**的 Markdown 正文
3. 都没有时 → 自动从 App Store 拉取 `releaseNotes`

顶部导航 **What's New**（`/changelog/`）会汇总各 App 的更新说明。

### 用法 A：本地 YAML（推荐，结构清晰）

在 `_apps/my-app.md` 的 `---` 之间加入：

```yaml
whats_new:
  - version: "1.2.0"
    items:
      - Added dark mode
      - Fixed crash on launch
  - version: "1.1.0"
    title: Improvements
    items:
      - Faster loading
      - New onboarding
```

也可用段落：

```yaml
whats_new:
  - version: "2.0"
    body: |
      Major rewrite.

      - New editor
      - iCloud sync
```

### 用法 B：本地 Markdown 正文

front matter 结束后直接写：

```markdown
---
layout: app
app_name: My App
ios_app_id: 1234567890
app_description: ...
---

## Version 1.2

- New feature
- Bug fixes
```

注意：一旦写了正文或 `whats_new`，就**不会**再自动拉 App Store 更新说明。

### 用法 C：完全自动（App Store）

不要写 `whats_new`，也不要在文件底部写正文。详情页和汇总页会通过 iTunes API 显示商店里的 Release Notes。

### 修改 / 清空本地 New

- 改内容：编辑 `whats_new` 或正文后 push  
- 改回自动拉取：删掉 `whats_new` 段，并清空 front matter 后的正文，只留 `---` 配置

---

## 6. 中英文切换文案

| 位置 | 英文字段 | 中文字段 |
|------|----------|----------|
| 首页大标题 | `_config.yml` → `page_title` | `page_title_zh` |
| 首页副标题 | `site_description` | `site_description_zh` |
| 各 App 卡片简介 | `_apps/*.md` → `app_description` | `app_description_zh` |
| 导航「更新日志」 | 页面 title `What's New` | 代码里已写死中文「更新日志」 |
| 法律页正文 | include 里 `data-lang="en"` | `data-lang="zh"` |

默认语言：**英文**。用户切换后保存在浏览器 `localStorage`（键名 `ekildar-site-lang`）。

---

## 7. 截图与预览视频

### 自动截图（默认开启）

`_config.yml` 中：

```yaml
auto_load_app_store_screenshots: true
```

会从 App Store 拉 **iPhone** 截图，详情页可左右切换。

### 使用本地截图（优先）

把图片放到：

```text
assets/screenshots/<slug>/
```

例如 Nexus Note：`assets/screenshots/nexus-note/1.png`  
支持：`.png` / `.jpg` / `.jpeg` / `.webp`

有本地图时，**不再**用 App Store 截图。

### 使用本地视频（最高优先）

放到：

```text
assets/videos/<slug>/
```

支持：`.mp4` / `.webm` / `.ogg`  
有视频时，预览区播视频，隐藏截图轮播。

### 关闭自动截图

在 `_config.yml` 或某个 App 的 front matter：

```yaml
auto_load_app_store_screenshots: false
```

---

## 8. 全站配置

文件：`_config.yml`

常用项：

| 配置 | 作用 |
|------|------|
| `url` / `baseurl` | 生产域名（自定义域名时 `baseurl: ""`） |
| `page_title` / `page_title_zh` | 首页标题 |
| `site_description` / `site_description_zh` | 首页副标题 |
| `ios_app_country` | 默认商店国家 |
| `cover_image`、各种 `*_color` | 主题外观 |
| `your_name`、社交用户名等 | 页脚展示 |
| `enable_smart_app_banner` | iOS Safari 智能 App 横幅 |

改完 push `master` 即可。

---

## 9. 自定义域名

当前：`ekildar.kdns.fr`

| 项 | 值 |
|----|-----|
| 仓库 `CNAME` 文件 | `ekildar.kdns.fr` |
| GitHub Pages → Custom domain | `ekildar.kdns.fr`（不要带 www） |
| DNS | `CNAME`：`ekildar` → `ekildar27.github.io`（建议灰云） |
| `_config.yml` | `url: "https://ekildar.kdns.fr"`，`baseurl: ""` |

DNS 通过后勾选 **Enforce HTTPS**。

---

## 10. 向原开源项目贡献

生产内容（你的 App、域名、SETUP）在 **`master`**。  
给原作者 PR 用脚本生成干净分支：

```bash
# 必须在 master，且工作区干净
bash scripts/contribute-upstream.sh
```

脚本会：

- 建/更新 `feature/multi-app-showcase`
- 去掉 CNAME、个人 `_apps`、个人 `_legal`、SETUP.md
- 换成 example-app + 通用配置
- force push 到 fork

已有 PR：https://github.com/emilbaehr/automatic-app-landing-page/pull/107  

脚本跑完会自动切回 `master`，线上站点不受影响。

---

## 11. 常见问题

**Q：改完了手机还是旧样式？**  
强制刷新；或等 1～2 分钟让 Actions 部署完成。CSS 链接带 `?v=时间戳`，一般刷新即可。

**Q：详情页页脚点隐私政策 404？**  
检查是否有 `_legal/<同一 slug>/privacy.md`，且 `app_slug` 与文件名一致。

**Q：首页中文简介没变？**  
确认写了 `app_description_zh`，并已切到「中文」。

**Q：What's New 还是商店旧文案？**  
说明本地没有 `whats_new`/正文，正在拉 App Store。要自定义就按第 5 节加本地配置。

**Q：想改条款正文但只影响一个 App？**  
当前模板是全站共用。需要「每 App 独立全文」时再说一声，可以再改一版布局。

**Q：误在贡献分支上改了生产站？**  
切回 `master`：`git checkout master`。生产只维护 `master`。

---

## 相关文件一览

```text
_config.yml                          # 全站配置
_config_dev.yml                      # 本地预览覆盖
_apps/<slug>.md                      # 每个 App
_legal/<slug>/privacy.md             # 隐私页入口
_legal/<slug>/terms.md               # 条款页入口
_includes/legal-privacy-placeholder.html
_includes/legal-terms-placeholder.html
_pages/changelog.md                  # What's New 汇总页
assets/screenshots/<slug>/           # 本地截图
assets/videos/<slug>/                # 本地视频
scripts/contribute-upstream.sh       # 上游 PR 脚本
CNAME                                # 自定义域名
```

有不清楚的步骤，按「快速对照表」找到对应章节即可。
